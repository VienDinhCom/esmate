---
import AuthLayout from "@frontend/components/layouts/auth-layout.astro";

import { SignInForm } from "@frontend/features/auth/components/signin-form";

if (Astro.locals.session !== null) {
  return Astro.redirect(`/`);
}
---

<AuthLayout>
  <SignInForm client:only="react" />
</AuthLayout>
