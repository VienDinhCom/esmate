---
import MainLayout from "@frontend/components/layouts/main-layout.astro";

import { TanStackSPA } from "@frontend/tanstack";

if (Astro.locals.session === null) {
  return Astro.redirect(`/auth/sign-in?redirect=${Astro.url.pathname}`, 302);
}
---

<MainLayout>
  <TanStackSPA client:only="react" />
</MainLayout>
