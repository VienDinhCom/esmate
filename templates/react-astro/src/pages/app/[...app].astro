---
import BaseLayout from "@frontend/shared/components/layouts/base-layout.astro";

import { TanStackSPA } from "@frontend/tanstack";

export const prerender = false;

if (Astro.locals.session === null) {
  throw Astro.redirect("/auth/signin");
}
---

<BaseLayout>
  <TanStackSPA client:only="react" />
</BaseLayout>
